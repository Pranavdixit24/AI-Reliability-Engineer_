# VITE_API_URL: Set this in production to point to the backend URL.
# If unset, defaults to '/api' which works with local Vite proxy or same-origin deployments.
VITE_API_URL=https://your-backend-url.onrender.com
